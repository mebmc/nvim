return {}
-- return {
--     'nvimtools/none-ls.nvim',
--     config = function(_, opts)
--         local none_ls = require('null-ls')
--
--         -- Configure yamllint with custom config
--         local yamllint = none_ls.builtins.diagnostics.yamllint.with({
--             filetypes = { 'yaml', 'yml', 'yaml.ansible' },
--             extra_args = { '-c', vim.fn.stdpath('config') .. '/.yamllint.yaml' },
--         })
--
--         none_ls.setup(vim.tbl_extend('force', opts or {}, {
--             sources = {
--                 yamllint,
--             },
--         }))
--     end
-- }
